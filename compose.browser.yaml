services:
  browser:
    profiles: [browser]
    build:
      context: .
      dockerfile: infra/browser.Dockerfile
    init: true
    shm_size: 1gb
    depends_on:
      gateway:
        condition: service_healthy
