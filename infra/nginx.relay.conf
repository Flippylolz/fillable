pid /tmp/nginx.pid;
error_log /dev/null;
events { worker_connections 256; }
stream {
  resolver 127.0.0.11 valid=10s ipv6=off;
  server {
    listen 3200;
    set $shared_edge wef-shared-edge-nginx-1:3200;
    proxy_connect_timeout 5s;
    proxy_timeout 120s;
    proxy_pass $shared_edge;
  }
}
