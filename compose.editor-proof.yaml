# Opt-in synthetic corpus API. Never include this override in production.
services:
  api:
    command: uvicorn app.documents.prototype:app --host 0.0.0.0 --port 8000 --reload --no-access-log
    volumes:
      - ./fixtures:/fixtures:ro
